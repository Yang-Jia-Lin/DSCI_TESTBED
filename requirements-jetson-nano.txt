# DSCI Testbed dependencies for JetPack 5.1.x / Python 3.8 / aarch64.
#
# Target verified from /etc/nv_tegra_release:
#   Jetson Linux R35.6.0 (JetPack 5.1.4, Ubuntu 20.04), Python 3.8.10
#
# IMPORTANT: install the JetPack-compatible CUDA build of torch and the matching
# torchvision build before installing this file. They are deliberately omitted
# because a normal `pip install torch torchvision` can replace the Jetson build
# with an incompatible or CPU-only wheel. The recommended JetPack 5.1.x pair is
# NVIDIA torch 2.1.0a0+nv23.06 with torchvision 0.16.0 built from source.
# Verify the pair before installing the remaining dependencies:
#
#   python -c "import torch, torchvision; print(torch.__version__, torchvision.__version__, torch.cuda.is_available())"
#   python -m pip install -r requirements-jetson-nano.txt
#
# MNN and the iperf3 executable are also intentionally omitted: MNN is an
# optional platform-specific backend, while iperf3 is installed with apt.

# Online Device / Edge / Cloud / Scheduler runtime
numpy==1.24.4
pandas==2.0.3
Flask==3.0.3
requests==2.32.3
psutil==6.1.1
Pillow==10.4.0
tqdm==4.67.1

# Offline preparation, profiling, evaluation, and plots
matplotlib==3.7.3
PyYAML==6.0.2
scipy==1.10.1
scikit-learn==1.3.2
fvcore==0.1.5.post20221221

# ViT / DeiT bundles and model downloads
timm==1.0.15
huggingface_hub==0.36.0

# ONNX export used by export_mnn_segments.py. The MNN converter/runtime itself
# remains a separate, platform-specific installation.
onnx==1.17.0
