# DSCI Testbed dependencies for a Python 3.10 virtual environment on Jetson Nano.
#
# IMPORTANT: install the JetPack-compatible CUDA builds of torch and torchvision
# before installing this file. They are deliberately omitted here because a
# normal `pip install torch torchvision` can replace the Jetson build with an
# incompatible or CPU-only wheel. Follow NVIDIA's PyTorch-for-Jetson matrix for
# the JetPack version installed on the Nano, then verify it with:
#
#   python -c "import torch, torchvision; print(torch.__version__, torchvision.__version__, torch.cuda.is_available())"
#   python -m pip install -r requirements-jetson-nano.txt
#
# The repository uses Python 3.10 syntax. MNN and the iperf3 executable are also
# intentionally omitted: MNN is an optional backend, while iperf3 is installed
# with the operating-system package manager.

# Online Device / Edge / Cloud / Scheduler runtime
numpy==1.26.4
pandas==2.3.3
Flask==3.1.3
requests==2.34.2
psutil==7.2.2
Pillow==12.0.0
tqdm==4.67.1

# Offline preparation, profiling, evaluation, and plots
matplotlib==3.10.8
PyYAML==6.0.3
scipy==1.15.3
scikit-learn==1.2.2
fvcore==0.1.5.post20221221

# ViT / DeiT bundles and model downloads
timm==1.0.28
huggingface_hub==1.23.0

# ONNX export used by export_mnn_segments.py. The MNN converter/runtime itself
# remains a separate, platform-specific installation.
onnx==1.21.0
